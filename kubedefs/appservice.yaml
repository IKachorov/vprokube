apiVersion: v1
kind: Service
metadata:
  name: vproapp-service
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: vproapp-port
  selector:
    app: vproapp
  type: ClusterIP